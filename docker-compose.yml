services:
  database:
    image: postgres
    container_name: localhost
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secretkey
      POSTGRES_DB: mydatabase
    volumes:
      - ./pg-data:/var/lib/postgresql/data

  app:
    build: .
    container_name: teste_tecnico_app_example
    ports:
      - '5000:5000'
    volumes:
      - .:/usr/app
    depends_on:
      - database

volumes:
  pgdata:
    driver: local
